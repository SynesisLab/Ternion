{
  "identifier": "default",
  "description": "Default capabilities for the main window and the capture overlay",
  "windows": ["main", "capture"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default"
  ]
}