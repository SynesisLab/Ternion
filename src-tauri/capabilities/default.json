{
  "identifier": "default",
  "description": "Default capabilities for the main window, the capture overlay, and the quick-ask palette",
  "windows": ["main", "capture", "quick"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "opener:default"
  ]
}